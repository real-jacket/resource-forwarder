{
  "name": "@resource-forwarder/extension-shell",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@resource-forwarder/rule-core": "workspace:*",
    "@resource-forwarder/shared-types": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
